# Book Library API

REST API for mobile book library application.

## Requirements
- PHP 8.2
- MySQL 8
- Composer

## Installation
1. Clone repository
2. Run `composer install`
3. Configure database in `.env` file
4. Run migrations: `php sql/migration.php`